export type Example = {
  name: string;
};
export type Data = {
  value: string;
};
export type User = {
  firstname: string;
  lastname: string;
};
export type Matter = {
  name: string;
};
export type Users = {
  name: string;
  email: string;
  photoURL: string;
  validator: {
    error: string;
    success: string;
  };
};
